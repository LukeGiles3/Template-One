---
import { Image } from "astro:assets";
import siteContent from "../../data/site-content.json";
import { getAssetByPath } from "../../utils/getAssetByPath";

const companyInfo = siteContent.companyInfo;
const headerContent = siteContent.components.header;
const footerContent = siteContent.components.footer;
const footerLogo = getAssetByPath(companyInfo.logo.image);
---

<footer class="siteFooter">
  <div class="siteFooterInner">
    <div class="footerBrand">
      <a href="/" class="footerLogoLink">
        <Image
          class="footerLogo"
          src={footerLogo}
          width={220}
          alt={companyInfo.logo.imageAlt}
          loading="lazy"
          decoding="async"
        />
      </a>
      <div class="footerBrandCopy">
        <h2>{companyInfo.businessName}</h2>
        <p>{footerContent.body}</p>
      </div>
    </div>

    <div class="footerColumn">
      <h3>Site Links</h3>
      <ul class="footerList">
        {headerContent.links.map((link) => (
          <li><a href={link.href}>{link.name}</a></li>
        ))}
      </ul>
    </div>

    <div class="footerColumn">
      <h3>Contact</h3>
      <ul class="footerList footerContactList">
        <li>
          <span>Phone</span>
          <a href={`tel:${companyInfo.phoneNumber}`}>{companyInfo.phoneNumber}</a>
        </li>
        <li>
          <span>Email</span>
          <a href={`mailto:${companyInfo.email}`}>{companyInfo.email}</a>
        </li>
        <li>
          <span>Area</span>
          <p>{companyInfo.location}</p>
        </li>
      </ul>
    </div>

    <div class="footerColumn">
      <h3>Hours</h3>
      <ul class="footerList footerHoursList">
        {footerContent.hours.map((item) => (
          <li>
            <span>{item.label}</span>
            <p>{item.value}</p>
          </li>
        ))}
      </ul>
    </div>
  </div>

  <div class="siteFooterBottom">
    <p>&copy; {new Date().getFullYear()} {companyInfo.businessName}. {footerContent.copyright}</p>
  </div>
</footer>

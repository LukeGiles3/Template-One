---
const { linkName, linkHref, isActive = false } = Astro.props;
---

<a
  href={linkHref}
  class:list={["nav-link", { "nav-link-active": isActive }]}
  aria-current={isActive ? "page" : undefined}
>{linkName}</a>
